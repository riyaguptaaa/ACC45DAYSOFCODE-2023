#include <stdio.h>
#include <math.h>

int main() {
    double number = 9.0;
    double result = sqrt(number);
    
    printf("The square root of %.1f is %.2f\n", number, result);
    
    return 0;
}
